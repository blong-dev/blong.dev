# secrets — never commit
.dev.vars
.env
*.key

# runtime log
chat.log

# dependencies (Phaser is loaded via CDN today; this is for later)
node_modules/

# os / editor cruft
.DS_Store
*.log
screenshots/

# internal positioning doc — never deploy or commit
portfolio.md
